# Prompt: fix visual mismatches only

```md
Исправь только визуальные расхождения между реализацией и макетом.

Контекст:
- Экран: <screen-name>
- DSL: <path-to-layout-dsl.ts|md>
- Pixso: <pixso-link>
- Реализация уже существует.

Current result:
- <что визуально не совпадает>
- <какой блок выглядит не так>
- <какой элемент лишний / отсутствует / неправильно выровнен>

Expected result:
- <как должно выглядеть по макету>

Scope:
- Исправить только alignment, spacing, sizing, grouping, visual emphasis, text wrapping или placement.

Out of scope:
- Не менять бизнес-логику.
- Не менять mapping.
- Не менять behavior, кроме случаев, когда визуальное состояние напрямую от него зависит.
- Не делать глобальный restyle.

Acceptance:
- Исправлены только перечисленные визуальные расхождения.
- Остальная структура страницы не меняется.
- Не появляется новый функционал.
- Код остаётся понятным, без лишнего refactor.

После правок:
- Коротко перечисли, какие visual fixes были внесены.
```
