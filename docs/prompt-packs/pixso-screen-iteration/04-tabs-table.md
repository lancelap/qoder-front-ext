# Prompt: implement only tabs and static table

```md
Реализуй только этап `TabsHeader + static table`.

Контекст:
- Дизайн DSL: <path-to-layout-dsl.ts|md>
- Pixso: <pixso-link>
- FiltersCard уже существует.

Scope:
- Сделать `TabsHeader` с `activeTab`.
- Показать бейджи у табов.
- Добавить кнопку `Обновить`.
- Сделать статическую таблицу с мок-данными.
- Добавить заголовки колонок и иконки сортировки без логики сортировки.

Out of scope:
- Не делать checkbox selection.
- Не делать `Выбрано N ...`.
- Не делать `loading/empty/error`.
- Не делать рефетч/API.

Acceptance:
- Табы переключаются визуально.
- Активный таб подсвечен по DSL.
- Таблица отображает мок-строки в нужной структуре.
- Колонка даты содержит primary и secondary text.
- Код компилируется и не ломает предыдущие этапы.

Constraints:
- Использовать типы строк таблицы.
- Не добавлять лишние abstractions.
- Не реализовывать поведение, которого нет в scope.

Верни только изменения этого этапа.
```
